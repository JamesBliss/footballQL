// schema
const competitions_schema = require('./competitions.schema');

// resolvers
const competitions_resolvers = require('./competitions.resolvers');

module.exports = {
  competitions_schema,
  competitions_resolvers
};
